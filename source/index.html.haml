---
title: d, tx | BBTTXU
body_class: index
---
/ %header#header
/   .title
/     %h1= link_to "Denton, TX", "#/"

#main
#calendar.row( data-bind="template: { foreach: calendar, name: 'day-template' }" )

%script#day-template( type="text/html")

  .calendar( data-bind="attr: { id: id }, css: count_class" )
    %header
      %h3
        %a.title( href="#/" ) Denton, TX
        %a.self( data-bind="attr: { href: some_link }" )
          %span.day( data-bind="text: day" ) ???
          %span.month( data-bind="text: month" ) ???
          %span.date( data-bind="text: date" ) ???

        %a.menu( href="#/" ) &#9776;
    .content
      .prev
        %p &laquo;
      .shows
        %ul.shows.small-block-grid-1.large-block-grid-2(data-bind="template: { foreach: count, as: 'show', name: 'show-template' }")

      .next
        %p &raquo;

%script#show-template( type="text/html")
  %li
    %div.meta
      %h6
        %span.time(data-bind="text: moment( show.starts_at ).format('ddd, hA') " ) ???
        %span.subhead.venue(data-bind="text: venue.name") ???
        %span.price.right( data-bind="text: show.price" ) ???

    .artists
      %ul.artists.small-block-grid-1( data-bind="template: { foreach: artists, name: 'artist-template' }")

%script#artist-template( type="text/html")
  %li(data-bind="text: name") ???
